import { StyleSheet } from "react-native-unistyles";

export const styles = StyleSheet.create({
  fill: {
    flexGrow: 1,
    flexShrink: 1,
    flexBasis: 0,
  },
  row: {
    flexDirection: "row",
  },
  keyboardScreen: {
    flexGrow: 1,
  },
});
